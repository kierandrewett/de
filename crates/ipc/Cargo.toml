[workspace]

[package]
name = "ipc"
version = "0.1.0"
edition = "2021"
description = "Shared IPC protocol types for compositor ↔ shell communication"
license = "MIT"
authors = ["Kieran Drewett"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
