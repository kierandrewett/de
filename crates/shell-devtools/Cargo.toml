[workspace]

[package]
name = "shell-devtools"
version = "0.1.0"
edition = "2021"

[features]
default = ["devtools"]
devtools = []

[dependencies]
iced = { version = "0.13", features = ["advanced"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
